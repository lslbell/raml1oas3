#%RAML 1.0
title: This is a sample api
version: 1.0
baseUri: https://api.example.com/v1

/types:
  get:
    description: Get all types
    responses:
      200:
        body:
          application/json:
            example: |
              [
                {
                  "id": 1,
                  "name": "Type 1"
                },
                {
                  "id": 2,
                  "name": "Type 2"
                }
              ]
